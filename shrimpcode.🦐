let bytes = 0;
{let a = ['', '', '', '', '', '', '', '', '', ''];

for(let i = 0; i < 1234; ++i) {
	for(let j = 0; j < 10; ++j) {
		a[j] = a[j] + 'asd';
	}
	
	bytes = bytes + collectGarbage();
}
}
println(bytes);