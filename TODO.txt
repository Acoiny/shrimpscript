Features to implement:

-NEXT: closures, like in lox/lua
-add destructuring syntaxes for lists (maybe instances)



-make global const checking runtime? => fix compiler "forgetting" consts when importing -- IN TESTING

-ASI, when illegal token is found, but preceded by line break, or followed by EOF or '}' -- IN TESTING

Discarded:


FIXES TO DO:
-find out if there is a memory leak in the GC

-improve error reporting

-stack trace -- IN TESTING